# Datei: Cargo.lock

## Zweck
Fixiert exakte Versionen der Abhängigkeiten für reproduzierbare Builds.

## Inhalt 1:1
- Enthält durch Cargo aufgelöste Dependency-Versionen.
- Wird bei `cargo build/run/check` automatisch aktualisiert.
- Enthält keine Projektlogik, nur Build-Auflösung.
